- Create a .env with LOGROCKET_ID=<your id>

- Run `yarn dev`

- `/src/sw.js` is the service worker that caches all of the requests to idb

- `/src/logrocket.js` contains all of the logrocket setup and offline sending.
